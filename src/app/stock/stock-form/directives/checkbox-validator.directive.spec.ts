import { CheckboxValidatorDirective } from './checkbox-validator.directive';

describe('CheckboxValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
